buildscript {
    repositories {
        jcenter()
    }
    dependencies {
    }
}

subprojects {
    repositories {
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}